# 빅데이터분석기사 작업형 3유형 - F-검정 및 합동분산추정량 문제 생성 프롬프트 (v1.0)

## 🎯 당신의 역할
당신은 빅데이터분석기사 실기시험 출제위원입니다. F-검정(분산 동질성 검정)과 합동분산추정량에 관한 작업형 3유형 예상문제를 생성해야 합니다.

---

## 🚨🚨🚨 최종 출력 필수 포함 사항 (절대 생략 금지!) 🚨🚨🚨

**생성하는 문제는 반드시 다음 구조를 모두 포함해야 합니다:**

```markdown
# 제목

## 시나리오
[2-3문장]

## 데이터 생성 코드
[완전한 리터럴 데이터]

## ❓ 질문

**1)** [구체적인 질문 - 반드시 1)로 시작]
- 출력 형식: [소수점 자릿수 등]

**2)** [구체적인 질문 - 반드시 2)로 시작]
- 출력 형식: [명시]

**3)** [구체적인 질문 - 선택사항]
- 출력 형식: [명시]

## 힌트
[라이브러리, 함수, 주의사항]

## 정답 코드
[완전한 Python 코드]
```

**❌ 절대 하지 말 것:**
- 시나리오만 쓰고 질문을 생략
- 줄글 형태로 "~을 분석하시오" 식의 모호한 지시
- 1), 2), 3) 번호 없이 질문 나열

---

## ⚠️ 절대 규칙 (반드시 준수)

### 규칙 1: 데이터 생성 방식
```python
# ✅ 올바른 형식 (완전히 생성된 리터럴 데이터 제공)
import pandas as pd

df = pd.DataFrame({
    '환자ID': [f'P{i:02d}' for i in range(1, 41)],
    '집단': ['A']*20 + ['B']*20,
    '로그리지스틴': [2.45, 3.12, 2.89, 3.45, 2.67, 3.21, 2.98, 3.56, 2.34, 3.78,
                     2.91, 3.34, 2.76, 3.15, 2.88, 3.42, 2.65, 3.28, 2.93, 3.51,
                     4.12, 3.89, 4.56, 3.78, 4.34, 3.95, 4.21, 3.67, 4.45, 3.82,
                     4.08, 3.91, 4.38, 3.72, 4.15, 3.88, 4.28, 3.98, 4.42, 3.85]
})
```

**필수 조건:**
- ✅ 최소 **40행 이상** (각 집단 최소 20개)
- ✅ **연속형 변수** 사용 (분산 계산 가능)
- ✅ pandas DataFrame 형태로만 생성
- ✅ **모든 데이터를 리터럴 값으로 완전히 작성** (생성 코드 아님)
- ❌ np.random 사용 절대 금지 (랜덤 생성 X)
- ❌ CSV 파일 생성 절대 금지
- ❌ 외부 파일 읽기 금지

### 규칙 2: 변수명은 무조건 한국어
```python
# ✅ 좋은 예
'환자ID', '집단', '로그리지스틴', '혈압', '콜레스테롤', '체중'

# ❌ 나쁜 예 (영어 사용 금지)
'patient_id', 'group', 'log_resistin', 'blood_pressure'
```

### 규칙 3: 출력 형식
- 마크다운(.md) 형식
- 명확한 섹션 구분
- 소수점 자리수 명시 (예: "소수점 셋째 자리에서 반올림")

### 🚨 규칙 4: 명확한 질문 구조 (가장 중요!)
**반드시 "❓ 질문" 섹션에 번호가 매겨진 구체적인 질문을 포함해야 합니다!**

```markdown
## ❓ 질문

**1)** 두 집단의 [변수명] 분산에 차이가 있는지 F-검정을 수행하고, 검정통계량을 소수점 셋째 자리에서 반올림하여 출력하시오.
- 단, 분자의 자유도가 분모의 자유도보다 크도록 할 것
- 출력 형식: 소수점 셋째 자리 반올림 (예: 2.456)

**2)** 두 집단의 [변수명]에 대한 합동분산추정량을 소수점 셋째 자리에서 반올림하여 출력하시오.
- 출력 형식: 소수점 셋째 자리 반올림 (예: 0.234)

**3)** 유의수준 0.05에서 두 집단의 분산이 같다고 할 수 있는지 판단하시오.
- 출력 형식: "같다" 또는 "다르다"
```

**절대 금지:**
- ❌ 시나리오만 제공하고 구체적인 질문이 없는 경우
- ❌ "분석하시오", "검정하시오" 등 모호한 지시
- ❌ 출력 형식을 명시하지 않는 경우

**반드시 포함:**
- ✅ 각 질문에 **1)**, **2)**, **3)** 번호 매기기
- ✅ 구체적인 작업 지시 (예: "F-검정을 수행하고 검정통계량을 출력하시오")
- ✅ 정확한 출력 형식 (예: "소수점 셋째 자리에서 반올림")
- ✅ 2-3개의 구체적인 질문

---

## 📋 필수 문제 구조 (순서 엄수)

모든 F-검정/합동분산추정량 문제는 **정확히 이 순서**를 따라야 합니다:

```
1. 제목 및 문제 유형 명시
2. 시나리오 (현실적이고 구체적으로)
3. 데이터 생성 코드 (40-50행)
4. 질문 (2-3개 소문항)
   - F-검정 통계량
   - 합동분산추정량
   - (선택) 결론
5. 힌트 (필수! 함수명, 사용법, 주의사항)
6. 정답 코드 (완전한 코드, 주석 포함)
```

---

## 📊 F-검정 및 합동분산추정량 이론

### F-검정 (분산 동질성 검정)

**언제 사용?**
- **두 집단의 분산이 같은지** 검정할 때
- 예시:
  - 두 약물 그룹의 반응 변동성 비교
  - 두 공정의 품질 일관성 비교
  - 두 집단의 측정값 분산 비교

**귀무가설과 대립가설:**
```
H0 (귀무가설): σ₁² = σ₂² (두 집단의 분산이 같다)
H1 (대립가설): σ₁² ≠ σ₂² (두 집단의 분산이 다르다)
```

**F-검정 통계량 계산:**
```python
# F = 큰 분산 / 작은 분산
# 또는: 분자 자유도가 분모보다 크도록 설정

# 방법 1: 직접 계산 (분자 자유도 > 분모 자유도)
if var1 > var2:
    F_stat = var1 / var2
    dfn = n1 - 1  # 분자 자유도
    dfd = n2 - 1  # 분모 자유도
else:
    F_stat = var2 / var1
    dfn = n2 - 1
    dfd = n1 - 1

# 방법 2: scipy 사용
from scipy import stats
# scipy.stats에는 직접적인 F-test 함수가 없으므로 직접 계산
```

**p-value 계산:**
```python
from scipy import stats

# 양측 검정
p_value = 2 * min(stats.f.cdf(F_stat, dfn, dfd),
                  1 - stats.f.cdf(F_stat, dfn, dfd))

# 또는 상측 검정 (F > 1인 경우)
p_value = 2 * (1 - stats.f.cdf(F_stat, dfn, dfd))
```

### 합동분산추정량 (Pooled Variance Estimator)

**언제 사용?**
- **두 집단의 분산이 같다고 가정**할 때
- 등분산 t검정의 전제조건
- 두 집단의 공통 분산을 추정

**공식:**
```python
# 합동분산추정량 (Pooled Variance)
s_p_squared = ((n1 - 1) * s1**2 + (n2 - 1) * s2**2) / (n1 + n2 - 2)

# 여기서:
# n1, n2 = 각 집단의 샘플 크기
# s1², s2² = 각 집단의 표본 분산 (ddof=1)
```

**합동표준편차:**
```python
s_p = np.sqrt(s_p_squared)
```

---

## 🎯 문제 생성 템플릿

### F-검정 및 합동분산추정량 문제 템플릿

```markdown
# F-검정 및 합동분산추정량 문제: [구체적 제목]

## 시나리오
[두 집단의 분산을 비교하는 상황 - 3-5문장]
- 어떤 연구/분석에서
- 두 집단을 비교하기 위해
- 어떤 연속형 변수를 측정했는지
- 분산의 동질성을 검정하려는 목적

## 데이터 생성 코드
```python
import pandas as pd

df = pd.DataFrame({
    '[ID변수]': [f'ID{i:02d}' for i in range(1, 41)],  # 40개
    '[집단변수]': ['[집단A]']*20 + ['[집단B]']*20,  # 각 20개
    '[측정변수]': [2.45, 3.12, 2.89, 3.45, 2.67, 3.21, 2.98, 3.56, 2.34, 3.78,
                   2.91, 3.34, 2.76, 3.15, 2.88, 3.42, 2.65, 3.28, 2.93, 3.51,
                   4.12, 3.89, 4.56, 3.78, 4.34, 3.95, 4.21, 3.67, 4.45, 3.82,
                   4.08, 3.91, 4.38, 3.72, 4.15, 3.88, 4.28, 3.98, 4.42, 3.85]
})

print(df)
print(f"\n집단별 샘플 수:")
print(df['[집단변수]'].value_counts())
```

## ❓ 질문

**1)** 두 집단의 [측정변수] 분산에 차이가 있는지를 알아보기 위해 F-검정을 수행할 때 검정통계량의 값을 구하시오.
- 단, 분자의 자유도가 분모의 자유도보다 크도록 할 것
- 출력 형식: 소수점 셋째 자리에서 반올림 (예: 2.456)

**2)** 두 집단의 [측정변수]에 대한 합동분산추정량을 구하시오.
- 출력 형식: 소수점 셋째 자리에서 반올림 (예: 0.234)

**3)** (선택) 유의수준 0.05에서 두 집단의 [측정변수] 분산이 같다고 할 수 있는지 판단하시오.
- 출력 형식: "같다" 또는 "다르다"

## 힌트

### 필요한 라이브러리
```python
import pandas as pd
import numpy as np
from scipy import stats
```

### 주요 함수 사용법
1. **집단 분리**
   ```python
   집단A = df[df['집단변수'] == '집단A']['측정변수']
   집단B = df[df['집단변수'] == '집단B']['측정변수']
   ```

2. **분산 계산**
   ```python
   # ddof=1 (표본분산)
   var_A = 집단A.var(ddof=1)
   var_B = 집단B.var(ddof=1)
   ```

3. **F-검정 통계량**
   ```python
   # 분자 자유도 > 분모 자유도
   if var_A > var_B:
       F_stat = var_A / var_B
   else:
       F_stat = var_B / var_A
   ```

4. **합동분산추정량**
   ```python
   n1, n2 = len(집단A), len(집단B)
   pooled_var = ((n1-1)*var_A + (n2-1)*var_B) / (n1 + n2 - 2)
   ```

5. **p-value (선택)**
   ```python
   p_value = 2 * (1 - stats.f.cdf(F_stat, dfn, dfd))
   ```

### 핵심 공식
```
F 통계량 = 큰 분산 / 작은 분산
합동분산 = ((n₁-1)s₁² + (n₂-1)s₂²) / (n₁ + n₂ - 2)
```

### 흔한 실수
❌ 분자/분모 자유도 순서 혼동
❌ ddof=0 사용 (모분산 계산) - ddof=1 사용해야 함!
❌ 작은 분산을 분자에 놓음
❌ 자유도 계산 오류 (n-1이 아닌 n 사용)

## 정답 코드
```python
import pandas as pd
import numpy as np
from scipy import stats

# 데이터 (위와 동일하게 완전한 리터럴 값 사용)
df = pd.DataFrame({
    '[ID변수]': [f'ID{i:02d}' for i in range(1, 41)],
    '[집단변수]': ['[집단A]']*20 + ['[집단B]']*20,
    '[측정변수]': [2.45, 3.12, 2.89, 3.45, 2.67, 3.21, 2.98, 3.56, 2.34, 3.78,
                   2.91, 3.34, 2.76, 3.15, 2.88, 3.42, 2.65, 3.28, 2.93, 3.51,
                   4.12, 3.89, 4.56, 3.78, 4.34, 3.95, 4.21, 3.67, 4.45, 3.82,
                   4.08, 3.91, 4.38, 3.72, 4.15, 3.88, 4.28, 3.98, 4.42, 3.85]
})

# 집단 분리
집단A = df[df['[집단변수]'] == '[집단A]']['[측정변수]']
집단B = df[df['[집단변수]'] == '[집단B]']['[측정변수]']

print("="*60)
print("F-검정 및 합동분산추정량 분석 결과")
print("="*60)

# 기초 통계량
print("\n[기초 통계량]")
n1, n2 = len(집단A), len(집단B)
var_A = 집단A.var(ddof=1)  # 표본분산 (ddof=1 중요!)
var_B = 집단B.var(ddof=1)

print(f"[집단A] n={n1}, 분산={round(var_A, 4)}")
print(f"[집단B] n={n2}, 분산={round(var_B, 4)}")

# 1) F-검정 통계량
print("\n[1] F-검정 통계량")
# 분자 자유도 > 분모 자유도가 되도록
if var_A > var_B:
    F_stat = var_A / var_B
    dfn = n1 - 1  # 분자 자유도
    dfd = n2 - 1  # 분모 자유도
    print(f"분자: 집단A (분산={round(var_A, 4)})")
    print(f"분모: 집단B (분산={round(var_B, 4)})")
else:
    F_stat = var_B / var_A
    dfn = n2 - 1
    dfd = n1 - 1
    print(f"분자: 집단B (분산={round(var_B, 4)})")
    print(f"분모: 집단A (분산={round(var_A, 4)})")

print(f"분자 자유도: {dfn}")
print(f"분모 자유도: {dfd}")
print(f"✓ F 통계량: {round(F_stat, 3)}")

# 2) 합동분산추정량
print("\n[2] 합동분산추정량")
pooled_var = ((n1 - 1) * var_A + (n2 - 1) * var_B) / (n1 + n2 - 2)
print(f"공식: ((n1-1)*s1² + (n2-1)*s2²) / (n1 + n2 - 2)")
print(f"     = (({n1}-1)*{round(var_A, 4)} + ({n2}-1)*{round(var_B, 4)}) / ({n1}+{n2}-2)")
print(f"✓ 합동분산추정량: {round(pooled_var, 3)}")

# 3) 결론 (선택)
print("\n[3] 결론 (유의수준 α=0.05)")
# 양측 검정 p-value
p_value = 2 * (1 - stats.f.cdf(F_stat, dfn, dfd))
print(f"p-value: {round(p_value, 4)}")

if p_value < 0.05:
    결론 = "다르다"
    print(f"✗ 두 집단의 분산이 {결론} (p-value = {round(p_value, 4)} < 0.05)")
else:
    결론 = "같다"
    print(f"✓ 두 집단의 분산이 {결론} (p-value = {round(p_value, 4)} ≥ 0.05)")

print("\n" + "="*60)
```
```

---

## 💡 추가 출제 가능 질문

1. **기본 F-검정**: "F 검정통계량을 구하시오."
2. **합동분산추정량**: "합동분산추정량을 구하시오."
3. **합동표준편차**: "합동표준편차를 구하시오." (`np.sqrt(pooled_var)`)
4. **p-value**: "F-검정의 p-value를 구하시오."
5. **자유도**: "분자와 분모의 자유도를 각각 구하시오."
6. **결론**: "유의수준 0.05에서 등분산 가정이 충족되는지 판단하시오."

---

## 🎯 문제 생성 체크리스트

문제를 생성한 후 다음 항목을 확인하세요:

### 필수 확인 사항
- [ ] 데이터가 40행 이상인가? (각 집단 20개 이상)
- [ ] np.random 사용하지 않았는가? (리터럴 값으로만 작성)
- [ ] 변수명이 모두 한국어인가?
- [ ] 힌트 섹션이 포함되었는가?
- [ ] 정답 코드가 완전한가?
- [ ] 소수점 자리수가 명시되었는가?
- [ ] "분자 자유도 > 분모 자유도" 조건이 명시되었는가?

### 품질 확인 사항
- [ ] 시나리오가 현실적인가?
- [ ] 질문이 **번호** (1), 2), 3))로 매겨져 있는가?
- [ ] 출력 형식이 명확히 지정되었는가?
- [ ] ddof=1 사용이 명시되었는가?
- [ ] 주석이 충분한가?

---

## 📝 완전한 예제 문제

```markdown
# F-검정 및 합동분산추정량 문제: 당뇨병 환자의 로그 리지스틴 분석

## 시나리오
한 의학 연구팀은 당뇨병 환자를 두 그룹(치료군 A, 치료군 B)으로 나누어 각각 다른 약물을 투여한 후, 혈중 로그 리지스틴(Log Resistin) 수치를 측정했습니다. 연구팀은 두 치료법에 따른 측정값의 분산이 같은지를 확인하고, 이후 분석을 위해 합동분산추정량을 계산하려고 합니다.

## 데이터 생성 코드
```python
import pandas as pd

df = pd.DataFrame({
    '환자ID': [f'P{i:02d}' for i in range(1, 41)],
    '치료군': ['A']*20 + ['B']*20,
    '로그리지스틴': [2.45, 3.12, 2.89, 3.45, 2.67, 3.21, 2.98, 3.56, 2.34, 3.78,
                     2.91, 3.34, 2.76, 3.15, 2.88, 3.42, 2.65, 3.28, 2.93, 3.51,
                     4.12, 3.89, 4.56, 3.78, 4.34, 3.95, 4.21, 3.67, 4.45, 3.82,
                     4.08, 3.91, 4.38, 3.72, 4.15, 3.88, 4.28, 3.98, 4.42, 3.85]
})

print(df)
print(f"\n치료군별 샘플 수:")
print(df['치료군'].value_counts())
```

## ❓ 질문

**1)** 두 치료군의 로그리지스틴 값의 분산에 차이가 있는지를 알아보기 위해 F-검정을 수행할 때 검정통계량의 값을 구하시오.
- 단, 분자의 자유도가 분모의 자유도보다 크도록 할 것
- 출력 형식: 소수점 셋째 자리에서 반올림 (예: 2.456)

**2)** 두 치료군의 로그리지스틴 값에 대한 합동분산추정량을 구하시오.
- 출력 형식: 소수점 셋째 자리에서 반올림 (예: 0.234)

## 힌트

### 필요한 라이브러리
```python
import pandas as pd
import numpy as np
from scipy import stats
```

### 핵심 공식
- **F 통계량**: F = 큰 분산 / 작은 분산
- **합동분산추정량**: s²_p = ((n₁-1)s₁² + (n₂-1)s₂²) / (n₁ + n₂ - 2)

### 주요 함수
```python
# 분산 계산 (ddof=1 필수!)
var_A = 치료군A['로그리지스틴'].var(ddof=1)
var_B = 치료군B['로그리지스틴'].var(ddof=1)

# F 통계량 (큰 분산 / 작은 분산)
F_stat = max(var_A, var_B) / min(var_A, var_B)

# 합동분산추정량
pooled_var = ((n1-1)*var_A + (n2-1)*var_B) / (n1 + n2 - 2)
```

### 주의사항
- ddof=1을 사용해야 표본분산이 계산됩니다!
- 분자 자유도가 분모 자유도보다 크게 하려면, 큰 분산을 분자에 놓으세요.

## 정답 코드
```python
import pandas as pd
import numpy as np

df = pd.DataFrame({
    '환자ID': [f'P{i:02d}' for i in range(1, 41)],
    '치료군': ['A']*20 + ['B']*20,
    '로그리지스틴': [2.45, 3.12, 2.89, 3.45, 2.67, 3.21, 2.98, 3.56, 2.34, 3.78,
                     2.91, 3.34, 2.76, 3.15, 2.88, 3.42, 2.65, 3.28, 2.93, 3.51,
                     4.12, 3.89, 4.56, 3.78, 4.34, 3.95, 4.21, 3.67, 4.45, 3.82,
                     4.08, 3.91, 4.38, 3.72, 4.15, 3.88, 4.28, 3.98, 4.42, 3.85]
})

# 집단 분리
치료군A = df[df['치료군'] == 'A']['로그리지스틴']
치료군B = df[df['치료군'] == 'B']['로그리지스틴']

# 기초 통계량
n1, n2 = len(치료군A), len(치료군B)
var_A = 치료군A.var(ddof=1)
var_B = 치료군B.var(ddof=1)

print(f"치료군 A: n={n1}, 분산={round(var_A, 4)}")
print(f"치료군 B: n={n2}, 분산={round(var_B, 4)}")

# 1) F-검정 통계량
if var_A > var_B:
    F_stat = var_A / var_B
else:
    F_stat = var_B / var_A

print(f"\n1) F 검정통계량: {round(F_stat, 3)}")

# 2) 합동분산추정량
pooled_var = ((n1 - 1) * var_A + (n2 - 1) * var_B) / (n1 + n2 - 2)
print(f"2) 합동분산추정량: {round(pooled_var, 3)}")
```
```

---

## ⚠️ 주의사항 및 흔한 실수

### 1. 분산 계산 시 ddof 설정
```python
# ❌ 잘못된 코드 (모분산 계산)
var = data.var()  # ddof=0 (기본값)

# ✅ 올바른 코드 (표본분산 계산)
var = data.var(ddof=1)  # 표본분산 필수!
```

### 2. F-검정 분자/분모 순서
```python
# ❌ 잘못된 코드 (작은 분산을 분자에)
F_stat = min(var_A, var_B) / max(var_A, var_B)  # F < 1

# ✅ 올바른 코드 (큰 분산을 분자에)
F_stat = max(var_A, var_B) / min(var_A, var_B)  # F > 1
```

### 3. 합동분산추정량 공식
```python
# ❌ 잘못된 공식
pooled_var = (var_A + var_B) / 2  # 단순 평균 아님!

# ✅ 올바른 공식 (가중 평균)
pooled_var = ((n1-1)*var_A + (n2-1)*var_B) / (n1 + n2 - 2)
```

---

이 프롬프트를 사용하면 F-검정과 합동분산추정량에 관한 실전형 문제를 생성할 수 있습니다.
