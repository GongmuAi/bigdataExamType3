# 빅데이터분석기사 작업형 3유형 - F-검정 및 합동분산추정량 문제 생성 프롬프트

## 🚨🚨🚨 [최우선] 질문 형식 - 반드시 Q1. Q2. Q3. 형식 사용! 🚨🚨🚨

```
## ❓ 질문

Q1. [질문] (출력: 소수점 X자리)
Q2. [질문] (출력: 소수점 X자리)
Q3. [질문] (출력: 예/아니오)
```

**⛔ 절대 금지:** **1)**, **2)** 형식 / "## 질문" (이모지 없이)
**✅ 반드시:** "## ❓ 질문" 헤더 / Q1. Q2. Q3. 형식 / (출력: ...) 명시

---

## 📋 문제 구조

```
# [제목]
## 시나리오
[두 그룹의 분산 비교 또는 합동분산 계산 상황]
## 데이터 생성 코드
```python
[각 그룹 15-20개씩, 총 30-40행]
```
## ❓ 질문
Q1. ... (출력: 소수점 X자리)
## 힌트
## 정답 코드
```

---

## ⚙️ 데이터 규칙

- ✅ 두 그룹, 각 15-20개 (총 30-40행)
- ✅ 리터럴 값 (np.random 금지)
- ✅ 변수명 한국어

```python
df = pd.DataFrame({
    '샘플ID': [f'S{i:02d}' for i in range(1, 41)],
    '그룹': ['A']*20 + ['B']*20,
    '측정값': [85, 90, 88, 92, 87, 89, 91, 86, 88, 90,
               84, 89, 87, 91, 88, 86, 90, 87, 89, 91,
               82, 87, 85, 89, 84, 86, 88, 83, 85, 87,
               84, 88, 86, 90, 87, 85, 89, 86, 88, 90]
})
```

---

## 📊 F-검정 및 합동분산추정량

### 1. F-검정 (등분산성 검정)

**공식:** F = s₁² / s₂² (큰 분산 / 작은 분산)

```python
import numpy as np

그룹A = df[df['그룹'] == 'A']['측정값']
그룹B = df[df['그룹'] == 'B']['측정값']

var_A = 그룹A.var(ddof=1)  # 표본분산
var_B = 그룹B.var(ddof=1)

F = var_A / var_B if var_A > var_B else var_B / var_A
```

**해석:**
- F값이 1에 가까우면 등분산
- F값이 크면 분산 차이 큼

### 2. 합동분산추정량 (Pooled Variance)

**공식:** s²_p = [(n₁-1)s₁² + (n₂-1)s₂²] / (n₁+n₂-2)

```python
n1 = len(그룹A)
n2 = len(그룹B)
s1_sq = 그룹A.var(ddof=1)
s2_sq = 그룹B.var(ddof=1)

s_p_sq = ((n1-1)*s1_sq + (n2-1)*s2_sq) / (n1 + n2 - 2)
s_p = np.sqrt(s_p_sq)  # 합동표준편차
```

---

## 💡 질문 예시

```
## ❓ 질문

Q1. 그룹A의 분산을 구하시오. (출력: 소수점 3자리)
Q2. 그룹B의 분산을 구하시오. (출력: 소수점 3자리)
Q3. F-검정 통계량 (큰 분산/작은 분산)을 구하시오. (출력: 소수점 3자리)
Q4. 합동분산추정량 s²_p를 구하시오. (출력: 소수점 3자리)
Q5. 합동표준편차 s_p를 구하시오. (출력: 소수점 3자리)
```

---

## 🎯 힌트

```markdown
## 힌트

### 필요한 라이브러리
- pandas, numpy

### 주요 함수
1. 표본분산: `.var(ddof=1)`
2. F통계량: `큰분산 / 작은분산`
3. 합동분산: `[(n₁-1)s₁² + (n₂-1)s₂²] / (n₁+n₂-2)`
4. 제곱근: `np.sqrt()`

### 흔한 실수
❌ .var() 사용 (모분산) → .var(ddof=1) 사용 (표본분산)
❌ F통계량 계산 시 작은 분산을 큰 분산으로 나눔
❌ 합동분산 공식에서 ddof 잘못 계산
```

---

## 💻 정답 코드

```python
import pandas as pd
import numpy as np

df = pd.DataFrame({...})

# 그룹 분리
그룹A = df[df['그룹'] == 'A']['측정값']
그룹B = df[df['그룹'] == 'B']['측정값']

# Q1-Q2: 분산
var_A = 그룹A.var(ddof=1)
var_B = 그룹B.var(ddof=1)
print(round(var_A, 3))
print(round(var_B, 3))

# Q3: F-검정
F = var_A / var_B if var_A > var_B else var_B / var_A
print(round(F, 3))

# Q4-Q5: 합동분산
n1, n2 = len(그룹A), len(그룹B)
s_p_sq = ((n1-1)*var_A + (n2-1)*var_B) / (n1 + n2 - 2)
s_p = np.sqrt(s_p_sq)
print(round(s_p_sq, 3))
print(round(s_p, 3))
```

---

## ✅ 체크리스트

- [ ] "## ❓ 질문" (이모지 포함)
- [ ] Q1. Q2. Q3. 형식
- [ ] .var(ddof=1) 사용 (표본분산)
- [ ] F = 큰분산/작은분산
- [ ] 합동분산 공식 정확
- [ ] print 최소화
